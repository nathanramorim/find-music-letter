# MCPs — find-music-letter

| MCP | Status | Usado por |
|-----|--------|-----------|
| context7 | indisponível | Builder, Specifier |
| git | indisponível | Orquestrador, Revisor |

Nenhum MCP apareceu disponível nesta sessão (Claude Code CLI) — não há ferramenta `context7` nem uma ferramenta dedicada de `git` MCP carregada; o agente usa `Bash`/`git` diretamente. `CLAUDE.md` recomenda configurar esses MCPs para o Claude Desktop, mas isso é independente do Claude Code. Reavalie (`/constitution`) sempre que migrar de runtime/sessão, já que o status pode mudar.

Ferramentas equivalentes usadas via `Bash` nesta sessão (não são MCP, mas cumprem o mesmo papel):
- `gh` (GitHub CLI) — PRs, autenticado
- `vercel` (Vercel CLI) — deploy, autenticado
- `forge-sdd` — telemetria e lições do Forge-SDD
